import { Search } from 'lucide-react'
import { useMemo, useState } from 'react'
import ServiceCard from '../components/ServiceCard'
import { services } from '../data/services'
export default function Services(){const [q,setQ]=useState(''); const filtered=useMemo(()=>services.filter(s=>`${s.name} ${s.category} ${s.short}`.toLowerCase().includes(q.toLowerCase())),[q]); return <section className="container-app py-8 sm:py-14"><div className="max-w-2xl"><p className="eyebrow">Katalog layanan</p><h1 className="mt-2 text-3xl font-black tracking-tight sm:text-4xl">Layanan keperawatan di rumah</h1><p className="mt-4 leading-7 text-slate-600">Pilih berdasarkan kebutuhan pasien. Harga merupakan estimasi awal dan dapat berubah sesuai kondisi, kebutuhan alat/bahan, serta hasil verifikasi.</p></div><div className="relative mt-8 max-w-xl"><Search className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400" size={19}/><input type="search" aria-label="Cari layanan" value={q} onChange={e=>setQ(e.target.value)} placeholder="Cari infus vitamin, lambung, tanda vital..." className="w-full rounded-2xl border border-slate-200 bg-white py-3.5 pl-11 pr-4 outline-none focus:border-teal-400 focus:ring-4 focus:ring-teal-50"/></div><div className="mt-8 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{filtered.map(s=><ServiceCard key={s.id} service={s}/>)}</div></section>}
