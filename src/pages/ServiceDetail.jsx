import { ArrowLeft, Check, Clock, ShieldAlert } from 'lucide-react'
import { Link, useParams } from 'react-router-dom'
import { formatRupiah, services } from '../data/services'
export default function ServiceDetail(){const {id}=useParams(); const service=services.find(s=>s.id===id); if(!service)return <div className="container-app py-20">Layanan tidak ditemukan.</div>; const Icon=service.icon; return <section className="container-app py-8 sm:py-14"><Link to="/services" className="inline-flex items-center gap-2 text-sm font-bold text-slate-500"><ArrowLeft size={16}/> Kembali ke layanan</Link><div className="mt-7 grid gap-8 lg:grid-cols-[minmax(0,1fr)_340px] xl:grid-cols-[minmax(0,1fr)_380px]"><div><div className="flex items-center gap-4"><span className="grid size-14 shrink-0 place-items-center rounded-2xl bg-teal-50 text-teal-700"><Icon/></span><div><p className="text-sm font-bold text-teal-700">{service.category}</p><h1 className="text-2xl font-black tracking-tight sm:text-3xl">{service.name}</h1></div></div><p className="mt-7 max-w-3xl text-base leading-7 sm:text-lg sm:leading-8 text-slate-600">{service.description}</p><div className="mt-9"><h2 className="text-xl font-black">Termasuk dalam layanan</h2><div className="mt-4 grid gap-3 sm:grid-cols-2">{service.includes.map(i=><div key={i} className="flex items-center gap-3 rounded-2xl border border-slate-200 bg-white p-4"><span className="grid size-7 shrink-0 place-items-center rounded-full bg-teal-50 text-teal-700"><Check size={15}/></span><span className="font-medium">{i}</span></div>)}</div></div>{service.requiresScreening&&<div className="mt-8 flex gap-3 rounded-2xl border border-amber-200 bg-amber-50 p-5 text-amber-900"><ShieldAlert className="shrink-0"/><div><p className="font-bold">Layanan memerlukan verifikasi</p><p className="mt-1 text-sm leading-6">Booking bukan persetujuan tindakan otomatis. Tim akan meninjau data pasien dan dokumen medis yang relevan sebelum kunjungan dikonfirmasi.</p></div></div>}</div><aside className="card h-fit p-6 lg:sticky lg:top-28"><p className="text-sm text-slate-500">{service.price == null ? 'Biaya layanan' : 'Estimasi mulai'}</p><div className="mt-1 text-3xl font-black">{formatRupiah(service.price)}</div><div className="mt-4 flex items-center gap-2 text-sm text-slate-500"><Clock size={16}/> {service.duration}</div><Link to={`/booking?service=${service.id}`} className="btn-primary mt-6 w-full">Booking layanan</Link><p className="mt-4 text-xs leading-5 text-slate-500">Biaya final dapat menyesuaikan kondisi pasien, lokasi, alat/bahan, dan kebutuhan tindakan.</p></aside></div></section>}
